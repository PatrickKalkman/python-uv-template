name: CI
on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6      # installs uv
      - run: uv pip sync                 # sync from uv.lock
      - run: uv pip install -e .
      - run: pytest -q
